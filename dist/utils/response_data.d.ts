export declare function generateResponse(status: true | false, status_code: number, message: string, data?: object): {
    status: boolean;
    status_code: number;
    message: string;
    data: object;
};
